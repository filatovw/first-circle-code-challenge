[project]
name = "tx-csv-gen"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "faker>=37.3.0",
]

[dependency-groups]
test = [
    "mypy>=1.16.0",
    "ruff>=0.11.13",
]
